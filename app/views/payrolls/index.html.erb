<h1>Nóminas</h1>

<% years_for_payrolls(@payrolls).each do |year| %>

  <h2><%= year %></h2>

  <table class="table table-striped table-bordered bootstrap-datatable datatable">
    <tr>
      <th>Nombre empleado</th>
      <th>Fecha de inicio</th>
      <th>Fecha de fin</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>

  <% @payrolls.by_year(year).each do |payroll| %>
    <tr>
      <td><%= payroll.employee.name %></td>
      <td><%= payroll.start_date %></td>
      <td><%= payroll.end_date %></td>
      <td><%= link_to 'Mostrar', payroll %></td>
      <td><%= link_to 'Edita', edit_payroll_path(payroll) %></td>
      <td><%= link_to 'Eliminar', payroll, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
  </table>
<% end %>
